# Disable pnpm's minimum-release-age supply-chain gate. Our lockfile is freshly
# resolved and pinned; newer pnpm versions otherwise reject just-published
# transitive deps (e.g. electron-to-chromium / node-releases) during install.
minimum-release-age=0
